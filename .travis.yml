language: node_js
node_js:
   - '12.13.0'
 # Travis-CI Caching
cache:
  directories:
    - node_modules
install:
    - npm install
script:
  - npm run build
after_script:
  - travis ci init
# E: Build LifeCycle
branches:
  only: 
    - master

